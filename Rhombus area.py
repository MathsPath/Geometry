base = float(input("Input a value for base: "))
height = float(input("Input a value for height: "))
area = ( base * height ) / 2

print(f"Rhombus with base = {base} and height = {height} has area = {area}")
